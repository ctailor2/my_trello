source 'https://rubygems.org'


# Core
gem 'rails', '4.2.6'
gem 'pg', '~> 0.15' # Use postgresql as the database for Active Record

# Frontend
gem 'sass-rails', '~> 5.0' # Use SCSS for stylesheets
gem 'uglifier', '>= 1.3.0' # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '~> 4.1.0' # Use CoffeeScript for .coffee assets and views
gem 'jquery-rails' # Use jquery as the JavaScript library
gem 'turbolinks' # Turbolinks makes following links in your web application faster
gem 'haml' # Use haml templating language
gem 'bootstrap', '~> 4.0.0.alpha3' # Use bootstrap frontend framework
gem 'rails-assets-tether', '>= 1.1.0' # Use tether for bootstrap tooltip positions
gem 'bower-rails', '~> 0.10.0' # Use bower to manage js dependencies
gem 'angular-rails-templates' # Use rails templates with angular

# Backend
gem 'grape'
gem 'grape-entity' # Use grape entities for restful object representation
gem 'hashie-forbidden_attributes'
gem 'bcrypt', '~> 3.1.7' # Use ActiveModel has_secure_password

group :development, :test do
  gem 'pry-byebug' # Call 'binding.pry' anywhere in the code to stop execution and get a debugger console
  gem 'web-console', '~> 2.0' # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'spring' # Spring speeds up development by keeping your application running in the background
  gem 'rspec-rails', '~> 3.3.3' # RSpec for backend testing
  gem 'annotate', '~> 2.6.0' # Annotate schema on models and model spec files
  gem 'jasmine-rails' # Jasmine for frontend testing
  gem 'shoulda-matchers', '~> 3.1' # Shoulda matchers for convenient testing
end

